import React, { Component } from "react";

class Lifecycle extends components {
    render() {
        console.log('Lifecycle render')
        return(
            <div>Lifecycle</div>
        )
    }
}

export default Lifecycle